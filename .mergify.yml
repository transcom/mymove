queue_rules:
  - name: default
    conditions:
      - "#approved-reviews-by>=1"

pull_request_rules:
  - name: merge using the merge queue
    conditions:
      - base=master
    actions:
      queue:
        name: default
