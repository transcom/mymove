/test/getMovesSince:
  get:
    tags:
    - moves
    summary: summary
    description: summary2
    operationId: movesSince
    produces:
    - application/json
    parameters:
    - in: body
      name: body
      required: true
      schema:
        properties:
          moveSinceDate:
            type: string
            description: moves retrieved since this date
            format: date-time
          numMoves:
            type: string
            description: number of moves to return
    responses:
      '200':
          description: Moves returned sucessfully
          schema:
              $ref: '../../definitions/pptas/GetMovesSinceResponse.yaml'
      '400':
        $ref: '../../responses/InvalidRequest.yaml'
      '401':
        $ref: '../../responses/InvalidRequest.yaml'
      '500':
        $ref: '../../responses/ServerError.yaml'
