description: Describes a basic service item subtype of a MTOServiceItem.
allOf:
  - $ref: 'MTOServiceItem.yaml'
  - type: object
    properties:
      reServiceCode:
        $ref: '../ReServiceCode.yaml'
    required:
      - reServiceCode
