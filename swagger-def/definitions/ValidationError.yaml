allOf:
  - $ref: 'ClientError.yaml'
  - type: object
    properties:
      invalid_fields:
        type: object
        additionalProperties:
          description: List of errors for the field
          type: array
          items:
            type: string
    required:
      - invalid_fields
