#! /bin/bash

set -eu -o pipefail

sql_dir="sql"

if [ ! -d "${sql_dir}" ]; then
  echo "This must be run from the project root." 1>&2
  exit 1
fi

name="$1"
timestamp="$(date +%s)"
path="${sql_dir}/V${timestamp}__${name}.sql"
touch "$path"
echo "$path"
