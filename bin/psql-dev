#!/bin/bash

set -eu -o pipefail

db_password="mysecretpassword"

command=""
if [[ -n "${*:-}" ]]; then
  command='-c '"$*"
fi

# We need word-splitting to pass the $command to psql properly.
# shellcheck disable=SC2086
exec psql postgres://postgres:${db_password}@localhost/dev_db $command
