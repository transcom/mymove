import React from 'react';
import { render } from '@testing-library/react';

import AddressUpdatePreview from './AddressUpdatePreview';

describe('AddressUpdatePreview', () => {
  it('does a thing', () => {
    render(<AddressUpdatePreview />);

    expect(true).toBe(true);
  });
});
